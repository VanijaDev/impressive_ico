contract('impCrowdsaleInitialValues', function(accounts) {
  it("should assert true", function(done) {
    var imp_crowdsale_initial_values = impCrowdsaleInitialValues.deployed();
    assert.isTrue(true);
    done();
  });
});
